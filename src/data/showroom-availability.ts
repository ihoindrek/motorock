/**
 * Legacy slug fallback — prefer Woo ACF `showroom_available` on each product.
 * Keep empty so catalog badges follow WordPress only.
 */
export const SHOWROOM_AVAILABLE_SLUGS = new Set<string>();
